services:
  jp-song-shadowing:
    build: .
    env_file:
      - .env
    environment:
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.6-luna}
    ports:
      - "3009:3000"
    volumes:
      - ./data:/data
    restart: unless-stopped
